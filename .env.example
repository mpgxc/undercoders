# Comentários via Giscus (GitHub Discussions) — OPCIONAL.
# O projeto já vem configurado em src/lib/constants.ts para o repositório
# mpgxc/undercoders. Defina estas variáveis apenas se quiser sobrescrever o
# repositório/categoria sem editar o código (ex.: ao fazer um fork).
# Todos os valores são identificadores PÚBLICOS (não são segredos), gerados em
# https://giscus.app.
#
# Copie este arquivo para .env.local (ignorado pelo git) ou configure na Vercel.

NEXT_PUBLIC_GISCUS_REPO_ID=
NEXT_PUBLIC_GISCUS_CATEGORY_ID=
# Nome da categoria de Discussions (padrão do código: "General")
NEXT_PUBLIC_GISCUS_CATEGORY=
